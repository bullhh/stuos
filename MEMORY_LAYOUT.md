# StuOS 内存布局说明

## 概述

本文件说明了StuOS操作系统的内存布局设计。通过链接脚本定义了内核在内存中的组织方式，确保各个段落按预期放置在正确的位置。

## 链接脚本结构

### 入口点定义
```
ENTRY(_start)
```
指定内核的入口点为[_start](file:///home/szy/work/stuos/stuos/target/aarch64-unknown-none-softfloat/debug/deps/stuos-87387506004b0719.stuos.34b671af-cgu.0.rcgu.o)函数。

### 内存区域定义
```
MEMORY
{
    RAM : ORIGIN = 0x40000000, LENGTH = 16M
}
```
定义了一个16MB的RAM区域，起始于0x40000000地址。

### 段落布局
1. **.text段** - 代码段
   - 包含入口点代码和所有可执行代码
   - 放置在RAM区域的起始位置

2. **.rodata段** - 只读数据段
   - 包含字符串常量等只读数据

3. **.data段** - 可读写数据段
   - 包含初始化的可变数据

4. **.bss段** - 未初始化数据段
   - 包含未初始化的全局变量和静态变量

5. **.stack段** - 栈段
   - 预留32KB的栈空间
   - 栈顶指针定义为`stack_top`

## 内存布局图示

```
0x40000000 +------------------+
           |      .text       |
           |   (代码段)       |
           +------------------+
           |    .rodata       |
           | (只读数据段)     |
           +------------------+
           |      .data       |
           | (可读写数据段)   |
           +------------------+
           |      .bss        |
           | (未初始化数据段) |
           +------------------+
           |     .stack       |
           |    (栈空间)      |
           +------------------+
0x41000000 |                  |
           |    (未使用)      |
           +------------------+
```

## 设计考虑

1. **地址选择**：选择0x40000000作为起始地址是为了避免与硬件寄存器区域冲突
2. **栈空间**：预留32KB栈空间以满足内核初始化期间的需求
3. **段落对齐**：各段落按16字节对齐以提高访问效率
4. **可扩展性**：布局设计允许未来添加更多功能段

## 使用方法

链接脚本会自动在构建过程中使用，无需手动干预。当需要修改内存布局时，只需编辑[linker.ld](file:///home/szy/work/stuos/stuos/linker.ld)文件即可。